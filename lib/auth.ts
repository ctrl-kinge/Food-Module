// NextAuth (Auth.js v4) configuration.
//
// Phase 0 stub. In Phase 1 this exports the `authOptions` (credentials
// provider + role on the JWT/session) consumed by the route handler at
// app/api/auth/[...nextauth]/route.ts and by server-side `getServerSession`.

export {};
