import PagePlaceholder from "@/components/PagePlaceholder";

export default function LoginPage() {
  return (
    <PagePlaceholder
      phase="Phase 1"
      title="Log in"
      description="Credentials login (Customer / Rider / Restaurant) lands here in Phase 1."
    />
  );
}
